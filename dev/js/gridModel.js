/**
 * Model for the grid, e.g. number of columns, gutter-width, columns-width;
 */
var Model = (function(){
  'use strict';

  return {
    numColumns: 4,
    gutterWidth: 32,
    gridDivisions: 12,
    addPrefixes: false  
  };

})();
